<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.donncha.assignment" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Menu" android:label="Menu" > </activity>
        <activity android:name="Register" android:label="Register"></activity>
        <activity android:name="Account" android:label="Account"></activity>
        <activity android:name="DebateList" android:label="DebateList"></activity>
        <activity android:name="SearchbyTopic" android:label="SearchbyTopic"></activity>
        <activity android:name="CreateDebate" android:label="CreateDebate"></activity>
    </application>

</manifest>
